import * as THREE from "three";
import { GLTFLoader } from "GLTFLoader";

// Loader de Texturas
export const textureLoader = new THREE.TextureLoader();

// Loader de GLTFs / GLBs
export const gltfLoader = new GLTFLoader();
